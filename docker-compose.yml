services:
  health:
    image: ghcr.io/a-mandala/mandala-health:latest
    build: .
    container_name: mandala-health
    ports:
      - "8020:8020"
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
